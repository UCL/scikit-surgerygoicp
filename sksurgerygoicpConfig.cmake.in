#/*============================================================================
#
#  SKSURGERYGOICP: Python wrapping of GO ICP
#
#  Copyright (c) University College London (UCL). All rights reserved.
#
#  This software is distributed WITHOUT ANY WARRANTY; without even
#  the implied warranty of MERCHANTABILITY or FITNESS FOR A PARTICULAR
#  PURPOSE.
#
#  See LICENSE.txt in the top level directory for details.
#
#============================================================================*/

# Compute the installation prefix from this sksurgerygoicpConfig.cmake file location.
get_filename_component(SKSURGERYGOICP_INSTALL_PREFIX "${CMAKE_CURRENT_LIST_FILE}" PATH)

set(SKSURGERYGOICP_VERSION_MAJOR "@SKSURGERYGOICP_VERSION_MAJOR@")
set(SKSURGERYGOICP_VERSION_MINOR "@SKSURGERYGOICP_VERSION_MINOR@")
set(SKSURGERYGOICP_VERSION_PATCH "@SKSURGERYGOICP_VERSION_PATCH@")
set(SKSURGERYGOICP_BUILD_SHARED "@BUILD_SHARED_LIBS@")
set(SKSURGERYGOICP_LIBRARIES "@SKSURGERYGOICP_LIBRARIES@")
set(SKSURGERYGOICP_C_FLAGS "@CMAKE_C_FLAGS@")
set(SKSURGERYGOICP_CXX_FLAGS "@CMAKE_CXX_FLAGS@")
set(SKSURGERYGOICP_EXE_LINKER_FLAGS "@CMAKE_EXE_LINKER_FLAGS@")
set(SKSURGERYGOICP_SHARED_LINKER_FLAGS "@CMAKE_SHARED_LINKER_FLAGS@")
set(SKSURGERYGOICP_MODULE_LINKER_FLAGS "@CMAKE_MODULE_LINKER_FLAGS@")

set(SKSURGERYGOICP_INCLUDE_DIRS "${SKSURGERYGOICP_INSTALL_PREFIX}/@SKSURGERYGOICP_INSTALL_INC_DIR@")
set(SKSURGERYGOICP_LIBRARY_DIRS "${SKSURGERYGOICP_INSTALL_PREFIX}/@SKSURGERYGOICP_INSTALL_LIB_DIR@")
set(SKSURGERYGOICP_README_FILE  "${SKSURGERYGOICP_INSTALL_PREFIX}/README.md")
set(SKSURGERYGOICP_LICENSE_FILE "${SKSURGERYGOICP_INSTALL_PREFIX}/LICENSE.txt")

set(SKSURGERYGOICP_USE_FILE "${SKSURGERYGOICP_INSTALL_PREFIX}/Usesksurgerygoicp.cmake")

# See: https://www.stack.nl/~dimitri/doxygen/manual/external.html
# And: I'm assuming this would only be used in Build folder.
set(SKSURGERYGOICP_DOXYGEN_TAG_PREFIX "Doxygen/SKSURGERYGOICP.tag")
set(SKSURGERYGOICP_DOXYGEN_TAG_FILE "@CMAKE_CURRENT_BINARY_DIR@/Doxygen/SKSURGERYGOICP.tag")
